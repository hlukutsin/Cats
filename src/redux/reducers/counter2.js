
const initialState = {
  isLoaded: false,
  arr: []
}

export default function reducerItems(state = initialState, action) {
 
  // switch(action.type){
  //     case 'ADD_CLICKER':
  //       return {
  //         items: action.payload
  //       }
  //   default:
      return state 
  // }

    
}